name: Build and Deploy

on:
  workflow_dispatch:

jobs:
  build:
    uses: ./.github/workflows/build.yml
    secrets: inherit
    permissions:
      packages: write

  deploy:
    needs: build
    uses: ./.github/workflows/deploy.yml
    secrets: inherit
